import React from "react";

const Home = () => {
  return (
    <div className="flex items-center justify-center h-full">
      <h1 className="text-center text-5xl">
        In this project React was used for frontend, PHP was used for backend and MySQL was used for database.
      </h1>
    </div>
  );
};

export default Home;
